'use strict';

import {mocha} from 'mocha/mocha';
import {sinon} from 'sinon/lib/sinon';
import {expect} from 'chai';

describe('collector spec tests', function() {

	it('should expect collector', function() {
		expect(1).to.be.exist;
	});
});
